---
name: moonlit-mirror-tarot
description: 打开“月下镜语”离线可视化牌桌，让用户在浏览器中选择塔罗牌，再由当前连接的语言模型结合问题、牌阵、牌位、正逆位和牌间关系完成结构化自省解读，并把结果实时回传到同一网页。用于用户提出抽塔罗牌、用牌阵梳理问题、需要可视化选牌，或明确要求当前模型而非网页固定规则来解读时。
---

# 月下镜语塔罗抽牌

本技能只负责安全抽牌、上下文传递和结果回传。不要调用指定模型、索要 API Key，也不要把牌面交给另一个模型；解读者就是当前任务连接的模型。

## 必须完成的流程

1. 把包含本文件的目录记为技能目录。运行：

   `python3 <技能目录>/scripts/bridge.py start`

   牌桌应自动在默认浏览器打开。保留命令返回的 `sessionFile`；若浏览器没有自动打开，把返回的 `url` 交给用户，但继续等待。

2. 立即告诉用户牌桌已经打开，请在页面完成设置、抽牌与揭示。随后运行：

   `python3 <技能目录>/scripts/bridge.py wait --session <sessionFile> --timeout 0`

   等待期间保持任务活跃；若终端调用进入后台，定期检查，但不要替用户选择卡牌。

3. 收到 `ReadingSelectionPayload` 后，完整阅读
   [解读协议](references/interpretation-protocol.md)。结合用户问题与所有牌位生成一个符合
   [结果结构](references/result-schema.json) 的 JSON 对象。

4. 回传时优先让发布命令从标准输入读取，避免把问题和结果写入项目：

   `python3 <技能目录>/scripts/bridge.py publish --session <sessionFile> --input -`

   将纯 JSON 写入该进程的标准输入并结束输入。也可把临时 JSON 文件路径传给 `--input`，但发布后必须删除该临时文件。

5. 只有当命令返回 `deliveredToPage: true` 时，才告诉用户解读已经回到同一个网页。页面确认后，本地会话会自动清理；不要立即关闭浏览器。

## 失败与取消

- 格式或牌位映射校验失败时，原抽牌仍然有效。根据错误修正 JSON，再对同一 `sessionFile` 运行 `publish`；绝不要求重新抽牌。
- 用户取消、浏览器关闭或明确不要继续时，运行：

  `python3 <技能目录>/scripts/bridge.py close --session <sessionFile> --reason user_cancelled`

- 等待超时不代表牌面丢失。只要会话仍有效，可以继续等待。
- 不要在聊天、日志或长期文件中复述会话令牌。不要把 `sessionFile` 当作永久记录。

## 不可越过的边界

- 不给“是／否”、吉凶分数、概率或确定性预言。
- 不读心，不把牌面当作他人的真实想法或现实证据。
- 医疗、法律、投资和心理危机场景只帮助整理问题，明确优先使用现实信息与专业支持。
- 语言使用“可能、值得留意、可以尝试”等开放措辞，同时保持具体，不要逐句复述关键词。
- 技能模式不写入主网页历史，也不把模型解读自动加入分享图。
